# ===============================
# Только секреты (всё остальное — в config-server/configs/monolith.yml)
# ===============================
spring.datasource.password=${DB_PASSWORD}
jwt.secret=${JWT_SECRET}
openai.api.key=${OPENAI_API_KEY}
