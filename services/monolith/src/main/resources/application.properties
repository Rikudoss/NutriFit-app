# ===============================
# Только секреты (всё остальное — в config-server/configs/monolith.yml)
# После C.2 монолит больше не парсит JWT — JWT_SECRET удалён из конфига.
# ===============================
spring.datasource.password=${DB_PASSWORD}
openai.api.key=${OPENAI_API_KEY}
